import clsx from "clsx";
import React from "react";

const Link = (props: { link: string; title: string; id: string }) => {
  return <div className="mt-4 flex flex-col space-y-4"></div>;
};

export default Link;
